{
  "name": "triple-h-contact-saver",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "check": "npm run check:syntax && npm test",
    "check:syntax": "find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.js"
  }
}
